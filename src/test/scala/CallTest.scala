import java.util.concurrent.TimeUnit

import org.scalatest.{FunSuite, Matchers}

import scala.concurrent.duration.FiniteDuration

class CallTest extends FunSuite with Matchers {

  test("Create and Stop a Call") {
//    val operator = new Attendant(1, Operator)
//    val call = new Call(1)
//    Thread.sleep(3000)
//    call.stop()
//    call.duration().gt(FiniteDuration(3, TimeUnit.SECONDS)) shouldBe true
  }

}
